/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "www.google.com",
            "images.unsplash.com",
        ]
    }
}

module.exports = nextConfig
